{
  "name": "mmust-industrial-iot",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && npm ci && npm run prisma:generate && npx prisma db push --accept-data-loss && node src/app.js",
    "postinstall": "cd backend && npm install"
  },
  "engines": {
    "node": "20.x"
  }
}
